<%= form_for(@sc_value) do |f| %>
  <% if @sc_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sc_value.errors.count, "error") %> prohibited this sc_value from being saved:</h2>

      <ul>
      <% @sc_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contract_id %><br>
    <%= f.text_field :contract_id %>
  </div>
  <div class="field">
    <%= f.label :key %><br>
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
