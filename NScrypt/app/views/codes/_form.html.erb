<%= form_for @code, :html => { :class => "form-horizontal code" } do |f| %>

    <% if @code.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@code.errors.count, "error") %> prohibited this code from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @code.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :version, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :version, :class => 'form-control' %>
    </div>
    <%= error_span(@code[:version]) %>
  </div>
  <div class="control-group">
    <%= f.label :code, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :code, :class => 'form-control' %>
    </div>
    <%= error_span(@code[:code]) %>
  </div>
  <div class="control-group">
    <%= f.label :contract_id, :class => 'control-label' %>
    <div class="controls">
        <%= f.select(:contract_id, options_from_collection_for_select(Contract.all, :id, :title)) %>
    </div>
    <%= error_span(@code[:contract_id]) %>
  </div>



  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            codes_path, :class => 'btn btn-default' %>

<% end %>
